apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: bakehouse-http
  labels:
    app: bakehouse
spec:
  type: NodePort
  selector:
    app: bakeouse
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30020